---
interface Props {
  path: string;
  slug: string;
  emoji: string;
  title: string;
  status: 'Done' | 'WIP' | 'Not Started';
  description: string;
}

const { title, description, emoji, slug, status, path } = Astro.props;

const colorByStatus = {
  Done: 'bg-emerald-100 border-emerald-300 text-emerald-800',
  WIP: 'bg-orange-100 border-orange-300 text-orange-800',
  'Not Started': 'bg-neutral-100 border-neutral-300 text-neutral-800',
};
---

<li
  class='text-[1rem] border rounded-md p-4 hover:scale-[1.02] active:scale-[.98] transition ease-in-out'
>
  <a
    href={path + '/' + slug}
    class='flex justify-center gap-4 hover:text-inherit'
  >
    <section class='flex flex-col gap-2 w-[100%]'>
      <header class='flex justify-between items-center'>
        <section class='flex justify-start items-center gap-2'>
          <span>{emoji}</span>
          <h4 class="font-bold text-slate-800">{title}</h4>
        </section>
        <span class={`text-sm px-2 py-1 rounded  border ${colorByStatus[status]}`}>
          {status}
        </span>
      </header>
      <p class='text-[1rem] text-slate-500'>
        {description}
      </p>
    </section>
  </a>
</li>
